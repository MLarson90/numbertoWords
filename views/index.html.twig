<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <title>number thing</title>
  </head>
  <body>
    <div class="container">
      <h1>My Name is Max, I like Turtles!</h1>
      <form action="/score" method="post">
        <div class = "form-group">
          <input class="form-control" name="user_input" min="1" max="99" type="number" placeholder="Enter a flippin' number, jeez!">
        </div>
        <button type="submit" class="btn btn-danger">Submit</button>
      </form>
      {% if result != null %}
        <h3>{{ result }}</h3>
      <a type="button" class="btn btn-default" href="/">Try Another Number</a>
      {% endif %}
    <div>
  </body>
</html>
